# M.2 散热装甲宽度调整

## 目标

将主板 M.2 散热装甲的宽度缩小约 9%，使其视觉比例更紧凑，同时仍完整覆盖 M.2 SSD。

## 约束

- 仅收窄散热装甲的横向宽度。
- 保持散热装甲的长度、厚度、中心位置和动画节点名不变。
- 保持 SSD 模型尺寸和安装锚点不变。
- 调整后散热装甲宽度必须仍大于 SSD 宽度，两侧保留少量覆盖余量。

## 验证

- 脚本语法检查通过。
- 尺寸断言确认散热装甲仍宽于 SSD。
- Blender 重新导出 USDC。
- iOS 项目编译、安装并启动到 iPhone 17 Pro Max 模拟器。
